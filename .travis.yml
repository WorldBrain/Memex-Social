if: branch != master AND type = push
language: node_js
node_js:
    - 14.16.0
install:
    - yarn bootstrap
script:
    - cd frontend/
    - yarn test
    - yarn lint
    - yarn build
cache:
    yarn: true
    directories:
        - node_modules
notifications:
    email: false
