service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      match /u/{userId} {
        match /htmlBody/{contentId} {
          allow read: if request.auth.uid == userId;
          allow write: if request.auth.uid == userId;
        }
        match /favIcon/{domain} {
          allow read: if request.auth.uid == userId;
          allow write: if request.auth.uid == userId;
        }
      }
    }
  }
}
