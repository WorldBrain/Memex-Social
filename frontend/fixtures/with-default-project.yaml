objects:
  projects:
    - $store: default
      name: WorldBrain
      slug: worldbrain
      shortDescription: Collective Intelligence for Web Research
  availableProjectPostTags:
    - $store: bounty
      project: {$ref: projects.default}
      label: Bounty
    - $store: software
      project: {$ref: projects.default}
      label: Software
    - $store: front-end
      project: {$ref: projects.default}
      label: Front-end
    - $store: styling
      project: {$ref: projects.default}
      label: Styling
    - $store: feedback
      project: {$ref: projects.default}
      label: Feedback
  projectPosts:
    - $store: bounty
      project: {$ref: projects.default}
      title: 'Bounty: Implement sidebar design overhaul'
      shortDescription: 'We just got in the new designs for the new sidebar! Details documented in GitHub issue, including finished designs'
    - $store: feedback
      project: {$ref: projects.default}
      title: 'Community Feedback: Memex collaboration features'
      shortDescription: "It's almost time for our team to start working on the collaboration feature to get closer to the vision of collaborative web research. But, we need your input."
  projectPostTags:
    - post: {$ref: projectPosts.bounty}
      tag: {$ref: availableProjectPostTags.bounty}
    - post: {$ref: projectPosts.bounty}
      tag: {$ref: availableProjectPostTags.software}
    - post: {$ref: projectPosts.bounty}
      tag: {$ref: availableProjectPostTags.front-end}
    - post: {$ref: projectPosts.bounty}
      tag: {$ref: availableProjectPostTags.styling}
    
    - post: {$ref: projectPosts.feedback}
      tag: {$ref: availableProjectPostTags.feedback}
